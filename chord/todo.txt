1. message forwarding
1.5 make sure the anchor node gets a fingertable
2.update finger table with changes in the network, very important
2.5 retreive data for specific key
3. heartbeat with nodes in finger table, no need for this, just only allow nodes to exit cleanly, professor recommended this was because it's easier
4. implement cli, do this some as we go because we need to do some testing


